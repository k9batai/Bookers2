<%= render 'users/list', user: @user %>
    <div class= "col-md-8 offset-md-1">
      <h2>Book dateil</h2>
      <table class= "table">
        <tbody>
          <tr>
            <td><%= link_to user_path(@user.id) do %><%= image_tag @book.user.get_profile_image(100,100) %> <br>
            <%= @user.name %> <% end %> </td>
            <td><%= link_to @book.title,book_path(@book) %></td>
            <td><%= @book.body %></td>
            <td><%= link_to "edit",edit_book_path(@book) %></td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
</div>

